import { siteSettings } from "./siteSettings";
import { hero } from "./hero";
import { project } from "./project";
import { post } from "./post";

export const schemaTypes = [siteSettings, hero, project, post];
